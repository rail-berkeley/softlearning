name: softlearning
channels:
- defaults
- conda-forge
dependencies:
- python=3.6.5
- pip>=19.0
- conda>=4.6
- patchelf=0.9  # only works on Linux; comment this line out on Mac
- pip:
    - -r ./requirements.txt
